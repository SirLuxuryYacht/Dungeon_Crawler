extends Node

signal take_damage(Damager,Damaged)

signal change_soundtrack(track)

signal pick_map_item(item_name)

signal use_item(item,User)

signal show_level_up(xp_type)

signal update_map_brightness(scene_root,CurrentMapNode)
